{
    "version": 2,
    "buildCommand": "npm run build",
    "functions": {
        "dist/main.js": {
            "includeFiles": "dist/**/*.sql"
        }
    },
    "routes": [{ "src": "/(.*)", "dest": "dist/main.js" }]
}
