{
    "version": 2,
    "buildCommand": "npm run build",
    "functions": {
        "dist/main.js": {
            "runtime": "@vercel/node",
            "includeFiles": "dist/**/*.sql"
        }
    },
    "routes": [{ "src": "/(.*)", "dest": "dist/main.js" }]
}
